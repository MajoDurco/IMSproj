#~/bin/bash

# Get source directory path
SOURCE_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
# Move to source directory
cd "$SOURCE_DIR"

#=============================================================

zip 03_xkissm01_xdurco00.zip *.cpp *.h *.hpp Makefile